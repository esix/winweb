{
  "name": "winweb",
  "private": true,
  "version": "0.0.0",
  "description": "A Win9x-style desktop in the browser running real Win32 C apps compiled to WebAssembly by its own lcc-wasm toolchain (no Emscripten).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:wasm": "bash scripts/build-wasm.sh",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
